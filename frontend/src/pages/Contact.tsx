import { useState } from 'react';
import { CheckCircle2, Mail, MapPin, Send } from 'lucide-react';
import { submitContactForm } from '@/lib/api';
export default function Contact() { const [form, setForm] = useState({ name: '', email: '', program: 'CUBE', message: '' }); const [status, setStatus] = useState(''); const [loading, setLoading] = useState(false); const submit = async (e: React.FormEvent) => { e.preventDefault(); setStatus(''); setLoading(true); try { await submitContactForm(form.name, form.email, `[${form.program}] ${form.message}`, 'admissions'); setStatus('Thanks! Your message has been sent to Devigners.'); setForm({ name: '', email: '', program: 'CUBE', message: '' }); } catch (err) { setStatus(err instanceof Error ? err.message : 'Unable to send your message.'); } finally { setLoading(false); } }; return <div><section className="bg-slate-950 pt-32 text-white"><div className="mx-auto max-w-7xl px-5 pb-16 lg:px-8"><p className="eyebrow eyebrow-dark">Admissions</p><h1 className="mt-3 text-5xl font-black sm:text-6xl">Let’s find the right <span className="text-red-400">program.</span></h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-300">Tell us where you are in your learning journey and our team can guide you toward CUBE or COHORT.</p></div></section><section className="bg-slate-50 py-16"><div className="mx-auto grid max-w-7xl gap-10 px-5 lg:grid-cols-[.8fr_1.2fr] lg:px-8"><div className="space-y-4"><div className="rounded-2xl bg-white p-6 shadow-sm"><Mail className="h-6 w-6 text-red-600" /><h2 className="mt-4 font-extrabold">Email</h2><p className="mt-1 text-sm text-slate-600">hello@devigners.net</p></div><div className="rounded-2xl bg-white p-6 shadow-sm"><MapPin className="h-6 w-6 text-red-600" /><h2 className="mt-4 font-extrabold">Location</h2><p className="mt-1 text-sm text-slate-600">Pakistan</p></div></div><form onSubmit={submit} className="rounded-3xl bg-white p-6 shadow-xl sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label><span className="label">Name</span><input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} className="field" placeholder="Your name" /></label><label><span className="label">Email</span><input required type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} className="field" placeholder="you@example.com" /></label></div><label className="mt-5 block"><span className="label">Program</span><select value={form.program} onChange={e => setForm({ ...form, program: e.target.value })} className="field"><option>CUBE</option><option>COHORT</option><option>Not sure yet</option></select></label><label className="mt-5 block"><span className="label">Message</span><textarea required rows={6} value={form.message} onChange={e => setForm({ ...form, message: e.target.value })} className="field resize-none" placeholder="Tell us what you want to learn…" /></label>{status && <div className="mt-5 flex gap-2 rounded-xl bg-red-50 p-4 text-sm font-semibold text-red-700"><CheckCircle2 className="h-5 w-5 shrink-0" />{status}</div>}<button disabled={loading} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-red-600 px-5 py-3.5 font-extrabold text-white hover:bg-red-700 disabled:opacity-60">{loading ? 'Sending…' : 'Send admissions message'} <Send className="h-4 w-4" /></button></form></div></section></div>; }
