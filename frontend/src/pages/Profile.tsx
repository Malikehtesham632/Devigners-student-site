import { useEffect, useState } from 'react';
import { Link, Navigate } from 'react-router-dom';
import { User, Mail, LogOut } from 'lucide-react';
import { getMe } from '@/lib/api';
type User = { name: string; email: string; };
export default function Profile() { const [user, setUser] = useState<User | null>(null); const [loading, setLoading] = useState(true); useEffect(() => { const token = localStorage.getItem('access_token'); if (!token) return setLoading(false); getMe(token).then(setUser).catch(() => { localStorage.removeItem('access_token'); setUser(null); }).finally(() => setLoading(false)); }, []); if (loading) return <div className="flex min-h-[70vh] items-center justify-center pt-20 text-slate-500">Loading profile…</div>; if (!user) return <Navigate to="/" replace />; return <div className="bg-slate-50 pt-32"><div className="mx-auto max-w-3xl px-5 pb-20 lg:px-8"><p className="eyebrow">Student account</p><h1 className="mt-2 text-4xl font-black text-slate-950">Welcome, {user.name}</h1><div className="mt-8 rounded-3xl bg-white p-7 shadow-sm"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-red-50 text-red-600"><User /></div><div><p className="font-extrabold text-slate-950">{user.name}</p><p className="text-sm text-slate-500">Devigners student account</p></div></div><div className="mt-6 flex items-center gap-3 text-slate-700"><Mail className="h-5 w-5 text-red-600" /> {user.email}</div><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link to="/" className="rounded-xl border border-slate-200 px-5 py-3 text-center text-sm font-extrabold text-slate-700">Back home</Link><button onClick={() => { localStorage.removeItem('access_token'); window.location.href = '/'; }} className="inline-flex items-center justify-center gap-2 rounded-xl bg-red-600 px-5 py-3 text-sm font-extrabold text-white"><LogOut className="h-4 w-4" /> Sign out</button></div></div></div></div>; }
